apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "livraria.fullname" . }}-test-connection"
  labels:
    {{- include "livraria.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "livraria.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
