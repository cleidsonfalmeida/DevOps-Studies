apiVersion: v1
kind: Service
metadata:
  name: {{ include "livraria.fullname" . }}
  labels:
    {{- include "livraria.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "livraria.selectorLabels" . | nindent 4 }}
